"""Board scrapers via python-jobspy (SPEC.md §3.4, Tier 3).

Off by default; requires an explicit config opt-in plus a startup warning.
No credential storage, no cookie replay, no CAPTCHA handling, no
authenticated-session scraping. Jittered pacing, descriptive User-Agent.
"""
