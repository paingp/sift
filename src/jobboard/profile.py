"""Resume -> structured profile + profile documents (SPEC.md §6.1).

Parses config/profile.yaml into skills, seniority band, domains, and hard
constraints, plus 3-5 focused prose paragraphs used for embedding.
"""
